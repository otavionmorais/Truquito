import styled from 'styled-components';

export const CardContainer = styled.div`
    &:hover {
        transform: scale(1.2);
        transition: transform 0.5s;
    }

    margin: 10px;
    cursor: pointer;

`;